[project]
name = "dagster-doc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mdx-truly-sane-lists>=1.3,<2",
    "zensical>=0.0.31",
]

[dependency-groups]
dev = [
    "zensical>=0.0.45",
]
